import { SUBJECTS } from '@/content';

export { SUBJECTS };

export const OFFLINE_SUBJECTS = [
	SUBJECTS.mathematics.name,
	SUBJECTS.physics.name,
	SUBJECTS.english.name,
	SUBJECTS.afrikaans.name,
	SUBJECTS.history.name,
];
